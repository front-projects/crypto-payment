export const submitForm = async (data, mode) => {
  console.log(mode);
  // mode = deposit, withdraw

  //data = {
  //   email: email,
  //   amount: amount,
  //   crypto: crypto,
  // };
  return "https://www.google.com/";
};
