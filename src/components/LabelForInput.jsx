// eslint-disable-next-line no-unused-vars
export default function LabelForInput({ icon, label }) {
  return (
    <div className="flex items-center gap-2">
      {/* <div>{icon}</div> */}
      <div>{label}</div>
    </div>
  );
}
